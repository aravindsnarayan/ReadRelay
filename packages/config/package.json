{
  "name": "@readrelay/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared configurations for ReadRelay monorepo",
  "scripts": {
    "lint": "echo 'No linting for config files'",
    "type-check": "echo 'No TypeScript for config package'"
  }
}
